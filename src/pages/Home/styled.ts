import styled from 'styled-components';

import { appContainer } from '@/constants/styles/commonStyles';

export const Wrapper = styled.div`
  ${appContainer}
`;
export const Main = styled.main``;
