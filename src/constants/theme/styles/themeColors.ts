import { Colors } from './colors';

export const enum ThemeColors {
  LIGHT_THEME_BG = Colors.WHITE,
  DARK_THEME_BG = Colors.BLACK,
  DARK_THEME_COLOR = Colors.WHITE,
  LIGHT_THEME_COLOR = Colors.BLACK
}
